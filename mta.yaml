_schema-version: "3.2"
ID: mtabyte1
version: 0.0.1
modules:
- name: mtabyte1-destination-content
  type: com.sap.application.content
  requires:
  - name: mtabyte1-destination-service
    parameters:
      content-target: true
  - name: mtabyte1_html_repo_host
    parameters:
      service-key:
        name: mtabyte1_html_repo_host-key
  - name: uaa_mtabyte1
    parameters:
      service-key:
        name: uaa_mtabyte1-key
  parameters:
    content:
      instance:
        destinations:
        - Name: mtabyte1router_mtabyte1_html_repo_host
          ServiceInstanceName: mtabyte1-html5-app-host-service
          ServiceKeyName: mtabyte1_html_repo_host-key
          sap.cloud.service: mtabyte1router
        - Authentication: OAuth2UserTokenExchange
          Name: mtabyte1router_uaa_mtabyte1
          ServiceInstanceName: mtabyte1-xsuaa-service
          ServiceKeyName: uaa_mtabyte1-key
          sap.cloud.service: mtabyte1router
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: mtabyte1-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: mtabyte1_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - mtabyte1project.zip
      name: mtabyte1project
      target-path: resources/
- name: mtabyte1project
  type: html5
  path: mtabyte1project
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: mtabyte1-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: mtabyte1-destination-service
    service-plan: lite
- name: mtabyte1_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: mtabyte1-html5-app-host-service
    service-plan: app-host
- name: uaa_mtabyte1
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: mtabyte1-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
